name: foxutils-items
version: '${project.version}'
main: me.foxils.foxutils.FoxutilsItems
api-version: '1.21'

commands:
  get:
    description: Get custom item using the item's key
    usage: /<command> <itemkey>
    permission: op
  listitems:
    description: Lists all currently registered items
    usage: /<command>
    permission: op